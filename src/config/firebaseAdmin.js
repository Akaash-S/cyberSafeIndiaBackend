// Firebase Admin SDK removed - using simple authentication instead
// Frontend handles Firebase authentication and sends user data to backend

module.exports = {
  // Placeholder for any future Firebase-related utilities
  // No admin SDK needed
};
